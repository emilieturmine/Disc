{% extends 'base.html.twig' %}

{% block title %}Liste des artistes{% endblock %}

{% block body %}
<div class= "container fluid">
<div class="container mt-4">
<div class="jumbotron text-center">
    <h1>Liste des artistes</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artiste in artistes %}
            <tr>
                <td>{{ artiste.id }}</td>
                <td>{{ artiste.nom }}</td>
                <td>{{ artiste.url }}</td>
                <td>
                    <a href="{{ path('app_artiste_show', {'id': artiste.id}) }}">Détails</a>
                    <a href="{{ path('app_artiste_edit', {'id': artiste.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Pas d'artiste</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_artiste_new') }}">Ajouter un artiste</a>

    </div>    </div>
    </div>

{% endblock %}
