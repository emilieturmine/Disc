{% extends 'base.html.twig' %}

{% block title %}Liste des disques {% endblock %}

{% block body %}
    <h1>Liste des disques</h1>
<div class= "container fluid">
<div class="container mt-4">
<div class="jumbotron text-center">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Annee</th>
                <th>Picture</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disque in disques %}
            <tr>
                <td>{{ disque.id }}</td>
                <td>{{ disque.titre }}</td>
                <td>{{ disque.Annee }}</td>
                <td>{{ disque.picture }}</td>
                <td>{{ disque.label }}</td>
                <td>
                    <a href="{{ path('app_disque_show', {'id': disque.id}) }}">Détails</a>
                    <a href="{{ path('app_disque_edit', {'id': disque.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Pas de disques</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_disque_new') }}">Ajouter un artiste</a>
     </div>    </div>
    </div>

{% endblock %}
