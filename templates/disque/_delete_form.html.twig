<form method="post" action="{{ path('app_disque_delete', {'id': disque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disque.id) }}">
    <button class="btn">Delete</button>
</form>
